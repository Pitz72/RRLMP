# Log Dev v0.0.5
**Data**: 2026-01-03
**Autore**: Antigravity (Assistant)

## Riepilogo
Implementato **Drag & Drop** dei file audio e caricamento nell'Engine.

## Modifiche
### UI & Store
- **MainGrid.tsx**: 
  - Integrazione evento `onDrop` per catturare path assoluto dei file (Electron File Object).
  - Chiamata sequenziale `addClip` -> `loadClip`.
- **useProjectStore.ts**: 
  - Aggiornata `addClip` per memorizzare il `path` reale del file.
  - Aggiornata per restituire l'oggetto `AudioClip` appena creato.
- **useAudioStore.ts**:
  - Aggiunta azione `loadClip`: Pre-carica l'audio per calcolarne la durata e aggiorna lo store di progetto.
  - Fix delle importazioni di tipo (`AudioClip` da `types`).

### Sistema
- **src/main/index.ts**: Disabilitata `webSecurity` nelle `webPreferences` per permettere il caricamento di risorse locali (`file://`) tramite tag `<audio>` e `fetch`.

## Prossimi Passi
- Verificare se il ducking automatico funziona (logica di base presente ma non testata).
- Gestione errori più robusta per file non supportati.
