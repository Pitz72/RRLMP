# v1.10.20 — A3 CHIUSA: validazione oggettiva su musica reale + persistenza confidence

**Data:** 2026-07-02

Versione **patch** — chiusura della Fase A dell'automix.

## Validazione A3 (docs/automix/VALIDAZIONE-A3.md)

L'utente non può stimare i BPM a orecchio → validazione **strumentale** con il codice compilato reale dell'app (`out/main/bpmDetection.js`) su **9 brani veri** della sua libreria. Tre misure indipendenti: self-consistency su 3 segmenti diversi del brano, grid score dell'offset (onset sui beat predetti vs onset medio), confronto coi BPM canonici noti.

**Verdetto: Fase A validata.**
- BPM centrato sui brani a beat regolare (Losing My Religion 125.8≈126, What I've Done 120=120, A-Punk 175.9≈175, ecc.), coerenza al decimale tra segmenti.
- Offset agganciato: grid score 2.4–6.9 su tutti i brani (fase casuale ≈ 1.0).
- Casi difficili si comportano come previsto: ballad 6/8 → confidence 0.30; Pink Floyd "Mother" (tempo variabile, registrata senza click) → segmenti incoerenti (0/3).

## ⚠️ Scoperta per la Fase D: soglia confidence 0.3 NON basta

"Mother" — il caso da NON mixare mai — esce con confidence **0.41**: passerebbe la soglia 0.3 ipotizzata nel piano. I brani buoni stanno a 0.53–0.80. **Soglia raccomandata: ≥ 0.5.**

## Persistenza `bpmConfidence` (conseguenza diretta)

La confidence era calcolata dal main ma **scartata** dopo il badge — e la Fase D ne ha bisogno per decidere il fallback. Nuovo campo `AudioClip.bpmConfidence?` scritto dai 2 call-site AutoBpm in MainGrid, propagato da `classifyBpmResult` (guardia sui non-finiti). Stesso gate `bpmCheckedV2` di v1.10.17: nessuna nuova versione del gate (la v1.10.17-19 non è mai stata rilasciata né usata in regia — nessuna clip reale ha il gate senza confidence).

## Test e stabilità

- +1 test (confidence propagata), 2 test esistenti allineati al nuovo campo nel `toEqual` esatto.
- Typecheck renderer **0**. Vitest **111/111**.
- **Fase A CHIUSA** → si può procedere con la Fase B (motore di transizione).
