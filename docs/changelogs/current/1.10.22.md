# v1.10.22 — Automix Section: vista full-screen + scaletta (Fase C, step C1)

**Data:** 2026-07-02

Versione **patch** — prima UI della sezione Automix (decisioni utente: TRANSIZIONE manuale + opzione auto a fine brano; struttura confermata). In C1 la vista è di sola lettura: deck e pulsantone arrivano con C2.

## `components/automix/AutomixView.tsx` (nuova)

- **Vista a schermo pieno alternativa alla board** (nessun routing nell'app: stato `showAutomix` in App.tsx, stesso pattern del pad FX). Tema Spectrum.
- **z-30**: sopra la board, SOTTO pad FX (40)/Keymapping (50)/modali (200) → gli FX restano utilizzabili dentro l'automix.
- **Scaletta = colonna Music** (ordine = ordine colonna): per ogni riga numero, nome, durata, **badge BPM** (tooltip con confidence), **indicatore di compatibilità col precedente**:
  - 🟢 verde = mix a tempo comodo (deviazione rate ≤4%),
  - 🟡 giallo = al limite (≤ cap 8%),
  - 🔴 rosso = si andrà di crossfade classico, con motivo leggibile ("BPM mancante", "beat incerto", "tempi troppo diversi", "beat non rilevato").
- Riga del brano in onda evidenziata (bordo/glow emerald, via `activeClips`).
- **STOP ALL nell'header** (chiama `stopAll`), X per tornare alla board. **ESC deliberatamente NON chiude la vista**: resta l'Emergency Stop globale (criterio 4 del piano).
- Toggle **"MIX"** in topbar accanto a FX (stesso stile/blur anti-retrigger; props `automixOpen`/`onToggleAutomix` da App.tsx).

## Motore: `assessCompatibility` (nuova, `automixEngine.ts`)

Valutazione pura della compatibilità tra due brani adiacenti (verde/giallo/rosso + rate o `ClassicReason`), octave-aware come `computeTempoMatchRate`. +5 test (soglie 4%/8%, octave 90↔175.9, tutti i motivi di fallback).

## Verifica (preview browser, store popolato con dati della validazione A3)

Scaletta con 5 casi reali renderizzata correttamente: 120→125.8 "mix a tempo (rate 0.954)" giallo, 125.8→175.9 rosso "tempi troppo diversi", "Mother" conf 0.41 rosso "beat incerto", clip non analizzata rossa "BPM mancante". Screenshot ok.

## Test e stabilità

- Typecheck renderer **0**. Vitest **137/137** (+5).
- Prossimo: C2 — deck IN ONDA/PROSSIMO, START, pulsantone TRANSIZIONE + orchestrazione (`setPlaybackRate` su StreamPlayer, controller, telemetria). ⚠️ L'audio reale si verifica solo in dev.
