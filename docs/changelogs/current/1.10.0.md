# v1.10.0 — Milestone: batch "UI regia" completo

**Data:** 2026-07-01

Promozione di versione **MINOR** che marca il completamento del batch di miglioramenti UI della board (interfaccia di regia). **Nessuna modifica di codice** rispetto a 1.9.8 (stesso precedente di v1.3.0): solo bump + documentazione.

## Cosa comprende il batch (Step 1→4)
- **1.9.5 — Allargamento modali**: Impostazioni/Impostazioni Clip/Keymapping/Playout Log più larghe per mostrare più contenuto e ridurre lo scroll.
- **1.9.6 — Scrollbar colonne**: scrollbar `.col-body` custom scura e integrata col colore della colonna.
- **1.9.7 — Minipad FX 5×5 + header compatto**: la colonna FX esce dalla griglia e diventa un pad flottante non-bloccante (toggle accanto a STOP ALL); `col-sfx` resta nel modello dati (motore invariato). Brand ridotto a "RLM PRO" per liberare spazio in topbar.
- **1.9.8 — Layout colonne configurabile**: attiva/disattiva le colonne della board da Impostazioni → Generali (preferenza globale in localStorage); nascondere è solo visivo (clip preservate, rotazione PRE-SHOW e keybind/MIDI attivi).

## Stato
- Typecheck renderer 0, Vitest 94/94. Tutti gli step verificati in **dev** dall'utente.
- ⚠️ **Da verificare in regia** (uso live reale): pad FX durante la diretta, layout con colonne nascoste, comportamento della rotazione PRE-SHOW con jingle/promo nascosti.

## Contesto
- Controllo Remoto / app Android **in pausa** a v1.9.4 (mDNS auto-discovery; manca solo il test su hardware reale).

## File
- `package.json` (1.9.8 → 1.10.0), questo changelog, `relazione.md`.
