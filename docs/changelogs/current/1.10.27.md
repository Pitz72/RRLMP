# v1.10.27 — Rifiniture topbar (6 riscontri dev) + APPROVAZIONE automix

**Data:** 2026-07-02

Versione **patch** — chiusura sessione. **L'utente ha APPROVATO l'automix** ("Mi sembra che funzioni bene. Lo farò testare ad altri ma per ora per me è approvato") — la MINOR 1.11.0 resta in attesa dei test di terzi.

## Rifiniture topbar (riscontri utente, tutti e 6)

1. **Versione allineata alla baseline** di "RLM PRO" (prima `self-end` approssimativo).
2. **"PRO" iridescente**: usa la classe `.pro` del tema (gradiente cyan→verde→ambra→rosso) — era un verde fisso hardcoded che ignorava la grafica prevista dal prototipo Spectrum.
3. **Menu FILE spostato in testa**, subito dopo titolo+versione.
4. **FIX "STOP ALL su due righe" all'apertura di FILE**: la classe `.tool` ha `width:34px` fissa; lo stato "aperto" del trigger la perdeva → shift di larghezza → wrap del testo e header più alto. Ora FILE/FX/MIX usano lo stesso box in entrambi gli stati (cambiano solo i colori) + `whitespace-nowrap`. Header verificato stabile a 66px con menu aperto/chiuso.
5. **STOP ALL → "ALL"** dentro l'etichetta rossa (icona stop + ALL), tooltip esplicito "STOP ALL — ferma tutto (Emergency Stop)". Comportamento e MIDI bind invariati.
6. **Nuovo menu "strumenti" (🔧)**: Annulla/Ripeti (con stato disabilitato e shortcut mostrate), MIDI Learn (stato attivo/countdown/numero controller; countdown e stato errore visibili anche a menu chiuso come badge sul trigger), Keybinds, Impostazioni, Info. In topbar restano SOLO i controlli da diretta: FILE · ARM+mic · VU · MASTER VOL · ALL · FX · MIX · 🔧.

## Spazio recuperato (misure empiriche a 1366px)

Master Vol w-32→w-24, Mic Vol w-20→w-16, gap 16→8px, Log icon-only, badge Auto-saved **flottante** sotto la topbar (zero larghezza, niente shift quando compare). Risultato: contenuto sinistro **1230px → ~915px**; a 1366px ora c'è **+18px di margine** a mic spento (prima −180px con overlap sui controlli destri). ⚠️ Con mic ARMATO servono ~72px extra: a 1366 esatti resta un filo stretto (~−54px), da ~1440px in su tutto comodo — se la regia è davvero 1366 il prossimo passo è la compattazione responsive dei meter (da decidere).

## Test e stabilità

- Typecheck renderer **0**. Vitest **137/137**.
- Verifica in preview a 1366px: header stabile con menu aperti, ordine FILE→ARM→VU→MASTER→ALL→FX→MIX→🔧, menu strumenti con item corretti (Annulla/Ripeti disabilitati a cronologia vuota, MIDI "n/d" senza controller), gradiente `.pro` attivo, baseline versione ±1px.
