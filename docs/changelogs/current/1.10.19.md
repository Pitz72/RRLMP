# v1.10.19 — Menu file in topbar (Task 2 sessione Automix, deciso con l'utente)

**Data:** 2026-07-02

Versione **patch** — decongestione topbar approvata dall'utente dopo la misura empirica (a 1366px i pulsanti file finivano sopra REC/orologio; a 1920px margine zero).

## Menu a tendina "FILE" (`GlobalControls.tsx`)

- I 6 pulsanti file (Nuovo / Salva / Salva con nome / Carica / Importa M3U / Esporta) sono ora voci di un **menu a tendina** con trigger unico "FILE ▾" nella stessa posizione del gruppo.
- **Handler estratti VERBATIM** dai vecchi `onClick` inline in 6 funzioni nominate (`handleNewProject`, `handleSaveProject`, `handleSaveAs`, `handleLoadProject`, `handleImportM3u`, `handleExportProject`) — zero cambi di logica, stessi guard isDirty/confirm/toast.
- **Indicatore modifiche non salvate**: pallino giallo pulsante sul trigger (prima era il pulsante Salva giallo) + voce "Salva" evidenziata in giallo nel menu.
- Chiusura: click su una voce, click fuori, **ESC in capture con stopPropagation** (pattern ESC-01 v1.4.13: chiudere il menu NON innesca l'Emergency Stop). Blur anti-retrigger sul trigger (pattern v1.10.3).
- Etichette voci: stesse chiavi i18n dei vecchi tooltip. MIDI/keybind/impostazioni/info restano pulsanti singoli (invariati).

## Misure dopo il fix (renderer in browser, shim `window.electron`)

- Contenuto naturale del gruppo sinistro: **1448px → 1230px** (−218px).
- **A 1920px ora ci sta comodamente** (prima margine ~zero).
- ⚠️ **A 1366px NON basta ancora**: restano ~300px di sforamento (Redo/FILE/MIDI/keybind/impostazioni/info finiscono sotto il gruppo destro). Se la regia usa davvero ~1366px serve un secondo intervento da decidere (candidati: VU meter/Master Vol compatti a viewport strette, oppure anche MIDI/keybind/info nel menu).

## Verifica funzionale (preview browser)

Menu aperto con le 6 voci corrette, chiusura con ESC e click fuori, riapertura, indicatore dirty. Screenshot verificato.

## Test e stabilità

- Typecheck renderer **0**. Vitest **110/110** (invariati: solo spostamento JSX + estrazione handler).
