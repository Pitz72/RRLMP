# v1.3.15 — Audit 2026-05-29: Blocco Cleanup (post-audit)

**Data:** 2026-05-29

## Contesto

Blocco di pulizia a basso rischio, ripreso dai cleanup opzionali annotati al termine dell'audit MEDIE/LIEVI (v1.3.9 → v1.3.14). Nessun cambio di comportamento runtime: solo struttura componente e rimozione di simboli morti. Coerente con il principio **stabilità > funzionalità**.

Typecheck: `tsconfig.main.json` / `tsconfig.preload.json` **0 errori**. Renderer: **da 13 → 8 errori** preesistenti — verificato baseline a HEAD (`c0c5916`) = 13, le modifiche **non aggiungono alcun errore** e ne **rimuovono 5** (i simboli morti TS6133 in `DebugOverlay`/`MainGrid`). Gli 8 residui sono i gap `window.electron` (es. `getPlatform`, `saveRecordingBuffer`), non toccati da questo blocco.

---

## Cleanup (2)

- **DragHandle estratto a componente top-level** (`WaveformEditor`): il componente `DragHandle` era definito **inline** nel body di `WaveformEditor` → nuova identità di componente a ogni render → React **remontava** i 4 handle (Trim Start/End, Intro, Outro) a ogni render dell'editor. Estratto a componente top-level `DragHandle` con props esplicite (`leftPct`, `marker`, `color`, `label`, `show`, `duration`, `dragging`, `startDrag`). Stateless: nessuno stato perso, drag dei marker invariato. Era il rinvio "DragHandle inline" annotato in v1.3.14.
- **Rimozione simboli inutilizzati**:
  - `DebugOverlay`: rimossi l'import `useProjectStore` (mai usato), la variabile `audioStoreState = useAudioStore.getState()` (assegnata e mai letta) e gli import React `useEffect`/`useState` (mai usati). File ora 0 errori TS6133.
  - `MainGrid`: rimossa la destrutturazione `addClip` da `useProjectStore()` (mai chiamata — il drop usa `addClipAtIndex`).

---

## Non eseguito — con motivazione

- **`suppressedClips` in `useAudioStore`**: la rimozione era condizionata a "rischio nullo". Verificato che lo stato è **attivamente scritto** (riga ~349, salvataggio volume originale) e **letto** (riga ~346, guardia `isAlreadySuppressed`) nel path `behavior === 'stacco'` del mix — **non** è codice morto. Mantenuto invariato: toccarlo nell'hot-path del mix non è giustificato.

---

## Rinviato (1) — invariato da v1.3.14

- **pathUtils UNC** (`toFileUrl`): supporto percorsi di rete UNC sul protocollo `media://`. Richiede modifica coordinata main + renderer e test su share di rete reale. Resta rinviato a sessione dedicata (criterio Output Device).

---

## File modificati

- `package.json` (1.3.14 → 1.3.15)
- `src/renderer/src/components/ui/WaveformEditor.tsx` (DragHandle top-level + props esplicite)
- `src/renderer/src/components/debug/DebugOverlay.tsx` (rimossi import/var inutilizzati)
- `src/renderer/src/components/layout/MainGrid.tsx` (rimossa destrutturazione `addClip`)
- `docs/changelogs/current/1.3.15.md` (questo file)
