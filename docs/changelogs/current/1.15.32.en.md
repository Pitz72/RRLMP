# v1.15.32 — The app speaks Italian and English

**Type:** change (languages) · **Date:** 2026-09-13

## What changes

Runtime Live Machine Pro is now available **in Italian and English only**. French, German, Spanish, Portuguese, Russian and Chinese have been removed from the whole application:

- interface and system windows (save, open, closing with unsaved changes, error messages);
- language selector on the welcome screen and in Settings;
- in-app quick guide;
- the **remote control** page opened from a tablet or a second computer;
- the **Manual** button, which opens the Italian or English manual.

This is the same choice already made for the user manual and the update notes.

## A more compact welcome screen

With only two languages, the side panel holding the selector was almost empty. It has been removed: the language is now picked from a **drop-down in the top-right corner** of the welcome screen, showing the flag and name of the active language. The window is narrower and fully centred.

The drop-down closes when you pick a language, click outside it or press ESC (which does not trigger STOP ALL here).

## If you used another language

If the app was set to one of the removed languages, on the first launch after the update **it opens in English** and that choice is remembered. You can switch to Italian at any time from the language selector.

The remote control page follows the language of the device's browser: Italian if the device is set to Italian, English otherwise.

## User manual updated

The manual, in Italian and English, is aligned with this version and has **new screenshots**, captured from the updated application: the welcome screen with the language drop-down, the interface with a track in the ON AIR banner, the control bar with the ARM button, and the settings with the Microphone tab.

New or corrected content:

- **ON AIR banner** (new section 3.2): what it shows, the timer colours, UP NEXT, and which track it picks when several are playing.
- **Smart Mic** (chapter 6) and the Settings **Microphone tab** (chapter 13), with the **ARM** button in the control bar.
- **F1, F2, F3… keys** (chapter 8): they launch the visible columns in the order you see them. The manual said they didn't exist.
- **Shortcuts suspended** while a window is open (chapter 8).
- **Restart and install** asks what to do with unsaved changes (chapter 12).
- **Export project with audio** only deletes files inside `audio/` folders created by RRLMP (chapter 10).
- **Remote control**: a warning when the port is in use, and pages from other sites are refused (chapters 11 and 14).
- **Welcome screen**: the *User Manual* button opens the PDF (the manual described it as disabled), and there is the *Quick Guide* (chapter 2).
- Version 1.15.32 on the cover, title page, contents and chapters.

## Also included, if you are updating from an earlier version

If you are updating from a version earlier than 1.15.31, this update also brings you everything below.

**v1.15.31 — A steady board while on air**
- The **ON AIR** banner is always there: with nothing playing it shows OFF AIR in muted colours; when a track starts it lights up at exactly the same size, without pushing the columns down.
- The timer, the OUTRO IN warning and the UP NEXT box have a fixed footprint; between two tracks in a sequence the banner holds the track that just ended for a moment instead of flashing.
- On clip cards, badges, timer and title no longer move when UP NEXT, FADE OUT, TRIM or the INTRO/OUTRO cues appear.

**Audio and playback**
- **v1.15.16 — The end-of-track fade is no longer cancelled.** Firing or stopping a voice, jingle or effect while a track was fading brought it back to full volume, then cut it off abruptly at the end of the file.
- **v1.15.20 — Tracks on network folders actually play.** Clips on a NAS or shared folder (`\\server\...`) looked normal but would not start.
- **v1.15.24 — Inconsistent trims no longer break playback.** A trim longer than the track left the clip with zero length, and a looping clip restarted endlessly; such trims are now reset when the project opens.
- **v1.15.30 — Smart Mic is back.** With the microphone armed, the music dips on its own when you speak. Designed for a USB microphone plugged straight into the computer: with USB mixers the input carries the whole programme and the detector triggers by itself (the warning is written in the Microphone tab).

**Projects, saving and export**
- **v1.15.17 — Export no longer deletes files that don't belong to it.** An existing `audio/` folder next to the project was emptied on the first export; now only the archive folder created by RRLMP is cleaned.
- **v1.15.18 — The project no longer marks itself as "modified".** The automatic analyses on opening (silence, loudness, BPM, tags) no longer turn on the unsaved-changes indicator.
- **v1.15.19 — "Restart and install" no longer loses the open project.** With unsaved changes, the app now asks before installing the update.
- **v1.15.22 — Saving no longer hides missing-file warnings.** After "Save as", clips whose file was unreachable went back to looking normal.

**Recording**
- **v1.15.21 — Recording no longer hangs if the drive fails.** An unplugged USB stick or a full disk left the app waiting forever; now the recording closes and keeps what was captured.

**Remote control**
- **v1.15.23 — Stronger PIN and third-party connections refused.** The PIN is generated with the system's cryptographic generator; connections opened by web pages from other sites are refused.
- **v1.15.25 — Remote control warns when it fails to start.** With the port already in use, the switch stayed on with no server running; now it turns back off and shows the reason.

**Keyboard and interface**
- **v1.15.26 — Function keys follow the columns you see.** F1, F2, … follow the grid order and remap when you hide a column. With the default layout F6 is PRE-SHOW, which could not be reached from the keyboard before.
- **v1.15.27 — Refinements.** Among others: Ctrl+Z and Delete no longer act on the rundown while a window is open (ESC always works); intro and outro markers realign after seeking back.

**Maintenance**
- **v1.15.28** — Removed the internal leftovers of the old "Stacco" behaviour from the audio engine, with no change in behaviour.
- **v1.15.29** — Updated the vulnerable library used by the auto-updater to read information about new versions.

## Verification

- Typecheck 0 errors across all 4 projects · Vitest suite **224/224**.
- Tested in preview with **French** saved as the language: the app restarts in English, remembers English, and the selector shows only English and Italiano. Switching to Italiano, the interface and quick guide are in Italian.
- Remote control page script is valid, with an English and Italian dictionary.

## Files changed

- `src/renderer/src/i18n.ts` (`en`/`it` only + `supportedLngs`: removed languages fall back to English)
- `src/main/i18nMain.ts` (system window dictionaries reduced to `en`/`it`)
- `src/main/remoteControlAssets.ts` (remote page dictionary reduced to `en`/`it`)
- `src/renderer/src/components/modals/WelcomeScreen.tsx`, `GeneralSettingsModal.tsx` (language selectors)
- `src/renderer/src/components/ui/FlagIcon.tsx`, `src/renderer/src/utils/manualLinks.ts`
- `src/renderer/src/assets/quick-guide/index.ts`, `it.md`, `en.md`
- `src/renderer/src/devMock.ts` (demo in Italian and English only)
- **Removed:** `locales/{fr,de,es,pt,ru,zh}.json`, `assets/quick-guide/{fr,de,es,pt,ru,zh}.md`, `assets/flags/{fr,de,es,pt,ru,cn}.svg`, `bandiere/{fr,de,es,pt,ru,cn}.svg`
- Manual (IT/EN sources, chapters 2 and 13): "eight languages" → Italian and English
- `README.md`, `docs/ARCHITECTURE.md`, `docs/INDEX.md`
- `package.json` / `package-lock.json` (1.15.31 → 1.15.32)
