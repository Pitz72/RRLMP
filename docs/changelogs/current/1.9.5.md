# v1.9.5 — Allargamento modali (UI regia)

**Data:** 2026-07-01

Versione **patch** (linea UI regia in corso, Step 1/4). Prima delle 4 richieste di miglioramento interfaccia: modali, scrollbar colonne, minipad FX, layout colonne configurabile. Progetto Controllo Remoto/Android messo in pausa.

## Cosa cambia
Solo **presentazione**, nessuna logica toccata. Allargate le modali ricche di contenuto per mostrare più informazioni ed evitare/ridurre lo scroll verticale:

- **Impostazioni** (`.settings` in `spectrum.css`): larghezza `min(960px, 90vw)` → `min(1240px, 95vw)`, altezza `80vh` → `85vh`.
- **Impostazioni Clip** (`.clipset`): larghezza `min(720px, 92vw)` → `min(1000px, 95vw)`.
- **Keymapping** (`KeymappingModal.tsx`): pannello `max-w-5xl` → `max-w-7xl` (+ `max-h-[90vh]` → `92vh`); nomi clip troncati `max-w-[200px]` → `max-w-[360px]`.
- **Playout Log** (`PlayoutLogModal.tsx`): pannello `max-w-3xl` → `max-w-6xl` (+ `max-h-[80vh]` → `85vh`); colonne nome/artista `max-w-[160px]` → `max-w-[320px]`.

Lasciati invariati i dialoghi piccoli (About, Update, Recording Export, MIDI Simulator, Rotation, Export Progress, conferme): allargarli non aggiunge contenuto utile.

## Test e stabilità
- Typecheck renderer: **0 errori**. Nessun impatto su `.lmp`, motore audio o impostazioni.
- Verificato in dev dall'utente (Impostazioni, Impostazioni Clip, Keymapping, Playout Log).

## Prossimi step (batch UI regia)
- Step 2: scrollbar delle colonne più scura/integrata.
- Step 3: colonna FX rimossa dalla griglia, FX in un minipad 5×5 (overlay non-bloccante); `col-sfx` resta nel modello dati (bus/polifonia/esenzioni invariati).
- Step 4: layout regia configurabile (attiva/disattiva colonne, preferenza **globale** in localStorage).

## File principali
- `src/renderer/src/styles/spectrum.css` (`.settings`, `.clipset`)
- `src/renderer/src/components/modals/KeymappingModal.tsx`
- `src/renderer/src/components/modals/PlayoutLogModal.tsx`
- `package.json` (1.9.4 → 1.9.5), questo changelog, `relazione.md`
