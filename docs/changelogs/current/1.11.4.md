# v1.11.4 — Rifinitura modale Impostazioni a due colonne

**Data:** 2026-07-02

## Contesto

Chiusura del pendente di v1.10.15 ("non sono perfetti ma vanno bene per ora", rifinitura richiesta dall'utente il 2026-07-02: "ok verifica e fai"). Solo presentazione, zero logica.

## Modifiche (`GeneralSettingsModal.tsx`)

- **Tab Generali — Controllo Remoto nella colonna sinistra**: la sezione era full-width SOTTO il grid a due colonne → finiva sempre sotto scroll, mentre la colonna sinistra (solo Lingua) aveva spazio morto rispetto a Layout regia (6 righe di toggle). Ora la sinistra è Lingua + divider + Controllo Remoto, la destra resta Layout regia. **A 1366×768 la tab entra tutta senza scroll** (prima no).
- **Tab Master Chain — card allineate**: il grid HPF | Glue era `items-start` → la card HPF (1 slider) restava un "gradino" più corta della Glue (select stile + slider). Ora `items-stretch`: stessa altezza.

## Verifica

In preview browser (porta 5199, stub `window.electron`): screenshot tab Generali e Master Chain a viewport desktop e a 1366×768 (collasso a colonna singola sotto 768px invariato, breakpoint `md:`). Typecheck renderer 0, Vitest 138/138.
